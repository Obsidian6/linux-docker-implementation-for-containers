# 🐧 Discover Linux

**Discover Linux** is a minimal, elegant landing page introducing users to the world of Linux — built with simplicity, performance, and open-source spirit in mind.

This project demonstrates how to serve a static HTML/CSS site using **Docker** and **Nginx**, with full compatibility for **Ubuntu WSL**, **Docker Desktop**, or any Linux host.

---

## 🚀 Quick Start (Static Site with Docker + Nginx)

### 1. Clone this repository
```bash
git clone https://github.com/<your-username>/discover-linux.git
cd discover-linux
